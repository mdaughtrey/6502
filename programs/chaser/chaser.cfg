MEMORY {
RAM1: start = $0000, size = $4000;
ROM1: start = $c100, size = $3000, file = "rom1.bin", fill=true;
}

SEGMENTS {
CODE: load = ROM1, type = ro;
DATA: load = RAM1, type = rw;
BSS: load = RAM1, type = rw;
}
